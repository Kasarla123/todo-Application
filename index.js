import ReactDOM from "react-dom";
import AlternativeTodoList from './App'

const root=ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <>
    <AlternativeTodoList/>
  </>
);
